#include <iostream>
#include <string>
#include <vector>
#include <array>
#include <string>
#include <iterator>
#include <cstring>

using namespace::std;

int main()
{
    cout << 12 / 3 * 4 + 5 * 15 + 24 % 4 / 2 << endl;
    cout << (((12 / 3) * 4) + (5 * 15) + ((24 % 4) / 2)) << endl;
    // Result in 91
    return 0;
}
